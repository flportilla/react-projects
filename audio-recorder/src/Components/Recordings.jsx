import React from 'react'
import '../Styles/Recordings.css'

function Recordings() {

  return (
    <>
      <h1>Recordings</h1>
      {

        // if (recordsList.lenght === 0) return

        // recordsList.map((record, index) => {
        //   return
        //   <audio src={record} controls></audio>

        // })
      }
    </>
  )
}

export default Recordings